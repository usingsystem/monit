<div class="datagrid">
  <h5>网站PING时延TopN</h5>
  <table cellspacing="0" >
    <thead>
      <tr>
        <%=th_tag "网站"%>
        <%=th_tag "PING平均时延"%>
      </tr>
    </thead>
    <% if @ping %>
      <tbody>
        <% if @ping.size > 0 %>
          <% @ping.each do |type| %>
            <tr>
              <%=td_tag type["name"]%>
              <%=td_tag ('%.2f' %(type["rta"]))%>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    <%end%>
  </table>
</div>
