Global setup:
  Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email zzdhidden@gmail.com
        
Next steps:
  mkdir monit-web
  cd monit-web
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:zzdhidden/monit-web.git
  git push origin master
